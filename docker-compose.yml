services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: accesscontrol_app
    restart: unless-stopped
    working_dir: /var/www/html
    env_file:
      - .env
    volumes:
      - ./:/var/www/html
      - storage_data:/var/www/html/storage
    depends_on:
      db:
        condition: service_healthy
    networks:
      - accesscontrol

  queue:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: accesscontrol_queue
    restart: unless-stopped
    working_dir: /var/www/html
    env_file:
      - .env
    command: php artisan queue:work --sleep=3 --tries=3 --timeout=90
    volumes:
      - ./:/var/www/html
      - storage_data:/var/www/html/storage
    depends_on:
      db:
        condition: service_healthy
    networks:
      - accesscontrol

  nginx:
    image: nginx:1.27-alpine
    container_name: accesscontrol_nginx
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    networks:
      - accesscontrol

  db:
    image: mysql:8.4
    container_name: accesscontrol_db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: accesscontrol
      MYSQL_USER: accesscontrol
      MYSQL_PASSWORD: accesscontrol_password
      MYSQL_ROOT_PASSWORD: root_password_change_me
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3307:3306"
    healthcheck:
      test:
        [
          "CMD",
          "mysqladmin",
          "ping",
          "-h",
          "localhost",
          "-u",
          "root",
          "-proot_password_change_me"
        ]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - accesscontrol

volumes:
  mysql_data:
  storage_data:

networks:
  accesscontrol:
    driver: bridge